# Mệnh đề `HAVING`
- Mệnh đề `HAVING` được thêm vào SQL bởi vì từ khóa `HAVING` không thể sử dụng với các hàm tổng hợp .
- Cú pháp `HAVING` :
    ```sql
    SELECT column_name(s) FROM table_name WHERE condition GROUP BY column_name(s) HAVING condition ORDER BY column_name(s);
    ```
    